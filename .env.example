# AinzStack environment variables

# App
# NEXT_PUBLIC_APP_URL: your local or deployed app URL.
NEXT_PUBLIC_APP_URL=http://localhost:3000
# NEXT_PUBLIC_APP_NAME: public brand/app name shown in UI.
NEXT_PUBLIC_APP_NAME=AinzStack

# Supabase
# Find these in Supabase Dashboard -> Project Settings -> API.
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# Service role key is server-only and must never be exposed in client code.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe
# Find API keys in Stripe Dashboard -> Developers -> API keys.
STRIPE_SECRET_KEY=your-stripe-secret-key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
# Webhook secret comes from Stripe Dashboard -> Developers -> Webhooks.
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
# Product price IDs come from Stripe products created in Test or Live mode.
STRIPE_PRO_PRICE_ID=price_pro_monthly
STRIPE_ENTERPRISE_PRICE_ID=price_enterprise_monthly

# Resend
# API key from Resend Dashboard -> API Keys.
RESEND_API_KEY=re_xxxxxxxxx
# Sender must be a verified domain/sender in Resend.
RESEND_FROM_EMAIL=AinzStack <noreply@yourdomain.com>

# Google OAuth (configured in Supabase dashboard)
# Optional if using Google provider with Supabase Auth.
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
